{
  "name": "interrupted_rebuild_leftovers",
  "family": "recovery_debris",
  "description": "A failed rebuild left a bad replacement database in place along with preserved backups and recovery artifacts.",
  "expected_classification": "recoverable",
  "expected_command_outcomes": [
    {
      "surface": "startup/open",
      "outcome": "success_with_auto_recovery"
    },
    {
      "surface": "create",
      "outcome": "success_with_auto_recovery"
    },
    {
      "surface": "doctor",
      "outcome": "reports_errors"
    },
    {
      "surface": "doctor --repair",
      "outcome": "fails_repeated_repair"
    },
    {
      "surface": "sync --status",
      "outcome": "status_in_sync"
    },
    {
      "surface": "history",
      "outcome": "success"
    }
  ],
  "source_hint": "Inspired by the large repo-root recovery dumps collected during real-world investigation.",
  "notes": [
    "The live obr.db is plain text on purpose; the interesting state is the preserved backup and recovery trail.",
    "Future replay tests can assert that recovery tooling prefers the preserved backups over the broken live file.",
    "Post-de5bc9bd fail-closed contract: the recovery/*.rebuild-failed marker means a prior rebuild's verification failed, so `doctor --repair` refuses and demands --allow-repeated-repair instead of silently re-rebuilding over the evidence."
  ]
}
