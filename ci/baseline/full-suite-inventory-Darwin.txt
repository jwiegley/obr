# Full-suite failure inventory — NOT A GATE BASELINE
#
# No script reads this file. It is deliberately NOT named
# `integration-failures-baseline-Darwin.txt`, because scripts/unit-gate.sh
# WOULD honour that name and doing so would silently weaken the curated
# integration gate from 'fully green' to 'no new failures' — see
# ci/baseline/README.md, 'no inventory'.
#
# What this is: the complete failure set of `cargo test --all-features
# --no-fail-fast`, the tier push CI does not run. ci.yml runs `--lib --bins`
# plus doctests; the tests/*.rs targets below run only in nightly.yml or when
# another job names them (obr-rh0). Recorded so the next change can be judged
# against a number instead of an impression.
#
# Provenance: branch port/v0.3.2, commit b867ed05, 2026-08-17. Release profile,
# --all-features, --no-fail-fast, macOS/arm64, canonical TMPDIR (the default
# /var/folders path is a symlink obr refuses), toolchain from
# `nix shell github:nix-community/fenix#complete.toolchain`.
# Totals: 149 targets, 22122 passed, 11 failed, 119 ignored.
#
# This is the first inventory taken on the v0.3.2 substrate. The prior record
# was commit 02663b0d on the 0.2.22 base: 149 targets, 22079 passed, 43 failed.
# NO NEW FAILURE NAMES. Every name below also appears in that record; 32 of its
# names are gone, including all four e2e_doctor_chokepoint failures, both
# vocabulary_lint failures, all three e2e_concurrency failures, both
# storage_deps cycle families and ten of the eighteen e2e_routing failures.
#
# Attributions:
#   src/lib.rs (2)       — the committed Darwin pair in
#                          unit-failures-baseline-Darwin.txt
#   bench_synthetic_scale — obr-oz9
#   e2e_routing (8)      — upstream, see docs/RESIDUALS.md
#                          '~23 e2e_routing failures'; a subset of the 18
#                          recorded at 02663b0d
#
# Seen once under full-suite load and NOT reproducible in isolation, so not
# listed as failures — they are load artifacts of running 149 targets at once,
# and both pass on their own target:
#   conformance_edge_cases::conformance::common::harness::tests::
#     test_artifact_logger_writes_and_cleans  ("stdout artifact missing")
#   repro_mergereport_determinism::
#     sync_merge_json_notes_are_byte_identical_across_processes
#     (scratch `beads_mem_<pid>_<n>.db` under TMPDIR fails to open)

[src/lib.rs]  (2)
  cli::commands::changelog::tests::test_git_tag_date_falls_back_for_lightweight_tags
  cli::commands::vcs::tests::non_utf8_jsonl_leaf_is_observed_without_lossy_argument_conversion

[tests/bench_synthetic_scale.rs]  (1)
  synthetic_ci_profile_benchmarks_graph_projection_workloads

[tests/e2e_routing.rs]  (8)
  e2e_routing_close_sets_invoking_workspace_last_touched_for_follow_up_reopen
  e2e_routing_defer_and_undefer_external_issue_via_main_workspace
  e2e_routing_delete_external_issue_via_main_workspace
  e2e_routing_delete_force_dry_run_reports_orphans_not_cascade
  e2e_routing_delete_preview_does_not_mutate_earlier_local_batch
  e2e_routing_show_mixed_no_db_batches_preserve_local_db_override
  e2e_routing_update_external_issue_via_main_workspace
  e2e_routing_update_sets_invoking_workspace_last_touched_for_follow_up_close
